/**
 * Utilitários para processamento de templates
 *
 * Este arquivo deve implementar:
 * 1. Sistema de templating simples usando template literals
 * 2. Substituição de variáveis em templates
 * 3. Processamento de arquivos de template
 * 4. Renderização de templates com contexto
 * 5. Validação de variáveis de template
 *
 * Funções:
 * - renderTemplate(template: string, variables: Record<string, any>): string
 * - processTemplateFile(filePath: string, variables: Record<string, any>): Promise<string>
 * - validateTemplateVariables(template: string, variables: Record<string, any>): boolean
 * - extractTemplateVariables(template: string): string[]
 *
 * Use template literals nativos do JavaScript para interpolação
 */
