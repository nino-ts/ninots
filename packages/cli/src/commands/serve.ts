/**
 * Comando: ninots serve
 *
 * Este comando deve:
 * 1. Servir aplicação compilada em modo produção
 * 2. Otimizar para performance
 * 3. Implementar logging de produção
 * 4. Suportar variáveis de ambiente
 * 5. Permitir configuração de cluster/workers
 *
 * Uso:
 * ninots serve [options]
 *
 * Opções:
 * --port: porta do servidor
 * --host: host do servidor
 * --workers: número de workers (para clustering)
 * --env: arquivo de variáveis de ambiente
 *
 * Deve garantir que o projeto foi compilado antes de servir
 */
