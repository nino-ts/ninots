/**
 * Comando: ninots create
 *
 * Este comando deve:
 * 1. Gerar arquivos de código baseados em templates
 * 2. Suportar diferentes tipos (controller, service, model, middleware, etc.)
 * 3. Aplicar convenções de nomenclatura
 * 4. Atualizar arquivos de índice/exports quando necessário
 *
 * Uso:
 * ninots create <type> <name> [options]
 *
 * Exemplos:
 * ninots create controller UserController
 * ninots create service EmailService --async
 * ninots create model User --with-validation
 *
 * Deve usar templates do diretório templates/components
 */
