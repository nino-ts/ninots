/**
 * Comando: ninots dev
 *
 * Este comando deve:
 * 1. Iniciar servidor de desenvolvimento
 * 2. Implementar hot reload/restart automático
 * 3. Fornecer logs detalhados
 * 4. Permitir configuração de porta e host
 * 5. Integrar com debugger se disponível
 *
 * Uso:
 * ninots dev [options]
 *
 * Opções:
 * --port: porta do servidor (default: 3000)
 * --host: host do servidor (default: localhost)
 * --watch: arquivos a serem observados
 * --no-reload: desabilitar hot reload
 *
 * Deve usar recursos nativos do Bun para watch e restart
 */
