/**
 * Comando: ninots init
 *
 * Este comando deve:
 * 1. Criar estrutura de diretórios para novo projeto Ninots
 * 2. Gerar arquivos básicos (package.json, tsconfig.json, etc.)
 * 3. Configurar scripts de desenvolvimento
 * 4. Instalar dependências básicas
 *
 * Uso:
 * ninots init <project-name> [options]
 *
 * Opções:
 * --template: template a ser usado (default: basic)
 * --git: inicializar repositório git
 * --install: instalar dependências automaticamente
 *
 * Deve implementar interface Command e usar templates do diretório templates/project
 */
