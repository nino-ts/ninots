/**
 * Comando: ninots test
 *
 * Este comando deve:
 * 1. Executar suíte de testes usando Bun.test
 * 2. Suportar diferentes padrões de arquivos de teste
 * 3. Gerar relatórios de cobertura
 * 4. Permitir filtros por arquivo ou teste específico
 * 5. Suportar modo watch para testes
 *
 * Uso:
 * ninots test [pattern] [options]
 *
 * Opções:
 * --watch: modo watch
 * --coverage: gerar relatório de cobertura
 * --reporter: formato do relatório (default: pretty)
 * --timeout: timeout para testes (default: 5000ms)
 *
 * Deve integrar com sistema de testes nativo do Bun
 */
