/**
 * Template para middleware
 *
 * Este arquivo contém o template para gerar middlewares Ninots.
 *
 * Variáveis disponíveis:
 * - ${className}: Nome da classe do middleware
 * - ${fileName}: Nome do arquivo (kebab-case)
 * - ${description}: Descrição do middleware
 * - ${beforeHandler}: Se executa antes do handler principal
 * - ${afterHandler}: Se executa após o handler principal
 *
 * Middlewares interceptam requisições HTTP para processamento adicional
 */
