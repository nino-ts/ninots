/**
 * Template para serviço
 *
 * Este arquivo contém o template para gerar serviços Ninots.
 *
 * Variáveis disponíveis:
 * - ${className}: Nome da classe do serviço
 * - ${fileName}: Nome do arquivo (kebab-case)
 * - ${description}: Descrição do serviço
 * - ${isAsync}: Se o serviço usa operações assíncronas
 * - ${dependencies}: Dependências injetadas
 *
 * Serviços devem implementar lógica de negócio reutilizável
 */
