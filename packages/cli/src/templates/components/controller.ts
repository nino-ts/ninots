/**
 * Template para controlador
 *
 * Este arquivo contém o template para gerar controladores Ninots.
 *
 * Variáveis disponíveis:
 * - ${className}: Nome da classe do controlador
 * - ${fileName}: Nome do arquivo (kebab-case)
 * - ${description}: Descrição do controlador
 * - ${methods}: Métodos HTTP a serem implementados
 *
 * Exemplo de uso interno:
 * const template = await getControllerTemplate();
 * const rendered = renderTemplate(template, {
 *   className: 'UserController',
 *   fileName: 'user-controller',
 *   description: 'Gerencia operações de usuário'
 * });
 */
