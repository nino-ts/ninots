/**
 * Sistema de logging para o CLI
 *
 * Este arquivo deve implementar:
 * 1. Diferentes níveis de log (info, warn, error, debug, success)
 * 2. Formatação com cores para melhor UX
 * 3. Controle de verbosidade
 * 4. Spinners para operações longas
 *
 * Funcionalidades:
 * - log.info(message: string): void
 * - log.error(message: string): void
 * - log.success(message: string): void
 * - log.warn(message: string): void
 * - log.debug(message: string): void
 * - spinner.start(message: string): void
 * - spinner.stop(): void
 *
 * Use apenas console.log nativo com códigos ANSI para cores
 */
