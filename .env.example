# Application Configuration
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Security - Generate a strong random key (e.g., using openssl rand -base64 32)
APP_KEY=

# Database Configuration (Example for SQLite with Ninorm)
DATABASE_URL="file:./dev.db"

# Add other configurations as needed (e.g., external services API keys)
